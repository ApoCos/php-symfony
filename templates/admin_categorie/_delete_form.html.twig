<form method="post" action="{{ path('app_admin_categorie_delete', {'id': categorie.id}) }}" onsubmit="return confirm('Êtes-vous certains de vouloir supprimer cet item ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('supprimer' ~ categorie.id) }}">
    <button class="btn">Supprimer</button>
</form>
